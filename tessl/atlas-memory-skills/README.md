# Atlas Memory Skills

This Tessl tile contains Markdown skills that teach Atlas how to use Neo4j as a founder memory graph.

Install locally when Tessl is available:

```powershell
tessl install file:./tessl/atlas-memory-skills
```

Each skill defines:

- Extraction focus
- Neo4j/Cypher query strategy
- Answer style
- Dashboard priority
- Evidence rules
